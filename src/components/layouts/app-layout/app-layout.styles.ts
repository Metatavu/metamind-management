import { createStyles } from "@material-ui/core";

export const styles = createStyles({

  root: {
    width: "100vw",
    height: "100vh",
    backgroundColor: "#ddd"
  },

  appBar: {
    zIndex: 1251
  }
});
